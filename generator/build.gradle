apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile project(path: ':lib-generator')

    compile project(':models')
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
